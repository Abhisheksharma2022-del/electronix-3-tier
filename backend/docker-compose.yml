version: '3.8'

services:
  backend:
    image: 489741402840.dkr.ecr.us-east-2.amazonaws.com/electronix-ecr:latest
    container_name: electronix-backend

    ports:
      - "5000:5000"

    env_file:
      - .env

    restart: unless-stopped