pipeline {
    agent{ label 'electronix' }

    stages{
        stage("Tools configure"){
            steps {echo "Tools installing pipeline"}
        }

        stage ("List Update-1")
        {
            steps {
                sh '''
                sudo apt-get update -y
                '''
            }
        }
    }
}